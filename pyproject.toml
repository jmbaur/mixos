[project]
name = "mixos"
dynamic = ["version"]
dependencies = ["varlink>=32.1.0"]

[build-system]
requires = ["setuptools"]

[tool.setuptools]
packages = ["mixos"]

[tool.setuptools.dynamic]
version = { attr = "version.version" }

[project.scripts]
mixos = "mixos:cli"
